#pragma once

#include <cstddef>

namespace shader_const {
    const size_t TRANSFORM_MAT_LOCATION = 0;
    const size_t VIEW_MAT_LOCATION = 1;
    const size_t PROJECTION_MAT_LOCATION = 2;
    const size_t USE_TEXTURE_LOCATION = 4;
    const size_t USE_COLOR_LOCATION = 5;
    const size_t FONT_COLOR_LOCATION = 3;
    
}