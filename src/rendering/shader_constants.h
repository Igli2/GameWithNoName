#pragma once

#include <cstddef>

namespace shader_const {
    const size_t WINDOW_BOUNDS_LOCATION = 0;
    const size_t OFFSET_LOCATION = 1;
    const size_t USE_TEXTURE_LOCATION = 2;
    const size_t USE_COLOR_LOCATION = 3;
}